<%-include("partials/header.ejs") %>

    <body>
        <div class="container">
            <div class="todoApp">
                <div class="heading">
                    <h1>
                        <%= date %>
                    </h1>
                    <h2>What do you need to get done today?</h2>
                </div>

                <form action="/addworktodo" method="post">
                    <input type="text" id="newTodo" name="todo" placeholder="Add to-do..." />
                    <button type="submit" value="Submit">
                        <img src="/images/plus_button.svg" alt="plus button">
                    </button>
                </form>

                <div class="todo-list">
                    <% if(locals.worktasks) { %>
                        <% worktasks.forEach(worktask=>{ %>
                            <form action="/deleteWorkTodo" method="post">
                                <li>
                                    <input type="checkbox" name="checkbox" id ="check" class="check-size check" onChange="this.form.submit()"
                                        value="<%= worktask._id %>" />
                                    <label for="check" class="strikethrough">
                                        <%= worktask.todo %>
                                    </label>
                                </li>
                            </form>
                            <% }) %>
                                <% } %>
                </div>
            </div>
        </div>
    </body>